---
"@supa-media/native-safety": patch
"@supa-media/testing": patch
---

Classify PostHog's current and legacy React Native replay bridges as native dependencies so pinned-runtime Expo apps must declare and gate them before shipping an OTA update.
