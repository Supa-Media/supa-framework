# @supa-media/* publish to the public npm registry (registry.npmjs.org).
# No scope routing needed (npm is the default). Publish auth comes from
# NODE_AUTH_TOKEN/NPM_TOKEN in CI or `npm login` locally — never commit a token here.
