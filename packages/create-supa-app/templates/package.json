{
  "name": "{{APP_SLUG}}",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:mobile": "node scripts/dev.js --mobile",
    "dev:convex": "node scripts/dev.js --convex",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "setup:secrets": "op inject -i .env.example -o .env.local",
    "claude:sync": "supa-claude sync"
  },
  "devDependencies": {
    "@supa-media/claude": "^0.2.0",
    "convex": "^1.31.3",
    "turbo": "^2.0.0",
    "typescript": "~5.9.2"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  }
}
