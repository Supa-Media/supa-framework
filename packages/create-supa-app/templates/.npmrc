# Flat node_modules so Metro can resolve transitive Expo deps (expo-modules-core, etc.)
node-linker=hoisted

# @supa/* are published to GitHub Packages
@supa:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
